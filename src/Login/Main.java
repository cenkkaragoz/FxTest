package Login;

/*
public class Main extends Application {

    @Override
    public void start(Stage primaryStage) throws Exception{
        Button btn = new Button();
        btn.setText("Selam Dünyalı.. Biz Mars'tan.");
        Label lbl = new Label();
        lbl.setText("This is Turkey");
        btn.setOnAction(new EventHandler<ActionEvent>() {
            @Override
            public void handle(ActionEvent event) {
            System.out.println("Dünyalı Biz Dostuz.");
            }
        });

        // Aşağıda fxml 'den çağırılan pencere öreneği mevcut..
        //
        // Parent root = FXMLLoader.load(getClass().getResource("Login.fxml"));
        // primaryStage.setTitle("Selam Dünyalı");
        // primaryStage.setScene(new Scene(root, 300, 275));
        // primaryStage.show();

        StackPane root = new StackPane();
        root.getChildren().add(btn);
        root.getChildren().add(lbl);



        Scene scene = new Scene(root, 300, 250);
        primaryStage.setScene(scene);
        primaryStage.setTitle("Dünyalı");
        primaryStage.show();
    }


    public static void main(String[] args) {
        launch(args);
    }
}
*/